name: HTML to APK Builder

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Repo Kodlarını İndir
      uses: actions/checkout@v4

    - name: Node.js Ortamını Kur
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Görsel Dönüştürücü ve Cordova Araçlarını Yükle
      run: |
        sudo apt-get update
        sudo apt-get install -y librsvg2-bin
        npm install -g cordova

    - name: Projeyi Oluştur ve Yapılandır
      run: |
        cordova create myApp com.barkodpacker.app BarkodPacker
        cd myApp
        cordova platform add android
        
        # HTML dosyalarını kopyala
        rm -rf www/*
        cp -r ../index.html www/

        # SVG ikonunu uygulama ikonu ve açılış ekranı (splash) için PNG'ye dönüştür
        mkdir -p res/icon/android
        mkdir -p res/screen/android
        rsvg-convert -w 512 -h 512 ../icon.svg -o res/icon/android/icon.png
        rsvg-convert -w 1024 -h 1024 ../icon.svg -o res/screen/android/screen.png

        # config.xml içine uygulama adını, ikonunu ve açılış ekranı ayarlarını yaz
        cat << 'EOF' > config.xml
        <?xml version='1.0' encoding='utf-8'?>
        <widget id="com.barkodpacker.app" version="1.0.0" xmlns="http://www.w3.org/ns/widgets">
            <name>BarkodPacker</name>
            <description>Barkod ve Stok Yönetim Sistemi</description>
            <author email="dev@barkodpacker.app" href="https://github.com">BarkodPacker Team</author>
            <content src="index.html" />
            <access origin="*" />
            <allow-intent href="http://*/*" />
            <allow-intent href="https://*/*" />
            
            <!-- Açılış ekranı (Splash Screen) Süre Ayarları -->
            <preference name="SplashScreen" value="screen" />
            <preference name="SplashScreenDelay" value="2000" />
            <preference name="FadeSplashScreen" value="true" />
            <preference name="FadeSplashScreenDuration" value="500" />
            <preference name="ShowSplashScreenSpinner" value="false" />

            <platform name="android">
                <allow-intent href="market:*" />
                <icon src="res/icon/android/icon.png" />
                <splash src="res/screen/android/screen.png" density="land-hdpi"/>
                <splash src="res/screen/android/screen.png" density="land-ldpi"/>
                <splash src="res/screen/android/screen.png" density="land-mdpi"/>
                <splash src="res/screen/android/screen.png" density="land-xhdpi"/>
                <splash src="res/screen/android/screen.png" density="land-xxhdpi"/>
                <splash src="res/screen/android/screen.png" density="land-xxxhdpi"/>
                <splash src="res/screen/android/screen.png" density="port-hdpi"/>
                <splash src="res/screen/android/screen.png" density="port-ldpi"/>
                <splash src="res/screen/android/screen.png" density="port-mdpi"/>
                <splash src="res/screen/android/screen.png" density="port-xhdpi"/>
                <splash src="res/screen/android/screen.png" density="port-xxhdpi"/>
                <splash src="res/screen/android/screen.png" density="port-xxxhdpi"/>
            </platform>
        </widget>
        EOF

        cordova build android --debug

        # APK dosyasını doğrudan BarkodPacker.apk olarak adlandır
        cp platforms/android/app/build/outputs/apk/debug/app-debug.apk platforms/android/app/build/outputs/apk/debug/BarkodPacker.apk

    - name: APK Dosyasını Kaydet
      uses: actions/upload-artifact@v4
      with:
        name: BarkodPacker
        path: myApp/platforms/android/app/build/outputs/apk/debug/BarkodPacker.apk
        if-no-files-found: error
        retention-days: 1
        
