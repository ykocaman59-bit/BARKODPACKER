name: HTML to APK Builder

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Repo Kodlarını İndir
      uses: actions/checkout@v4

    - name: Node.js Ortamını Kur
      uses: actions/setup-node@v4
      with:
        node-version: 18

    - name: Cordova ve Paketleme Araçlarını Yükle
      run: |
        npm install -g cordova

    - name: Projeyi Oluştur ve Yapılandır
      run: |
        cordova create myApp com.barkodpacker.app BarkodPacker
        cd myApp
        cordova platform add android
        
        # HTML dosyalarını kopyala
        rm -rf www/*
        cp -r ../index.html www/
        
        # Eğer depoda icon.png varsa ikon olarak ayarla
        if [ -f "../icon.png" ]; then
          mkdir -p res/icon/android
          cp ../icon.png res/icon/android/icon.png
        fi

        cordova build android --debug

    - name: APK Dosyasını Kaydet
      uses: actions/upload-artifact@v4
      with:
        name: BarkodPacker-APK
        path: myApp/platforms/android/app/build/outputs/apk/debug/app-debug.apk
        
